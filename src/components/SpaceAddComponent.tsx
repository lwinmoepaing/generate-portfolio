import React from "react";

export default function SpaceAddComponent() {
  return (
    <div
      className="space-add-component"
      onClick={() => {
        // alert("Lwin");
      }}
    >
      Space Add Component
    </div>
  );
}
